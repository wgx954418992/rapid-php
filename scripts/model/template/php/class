<?php

${namespace}

${imports}

/**
 * ${tableComment}
 * @table ${tableName}
 * rapidPHP auto generate Model ${date}
 */
${classAnnotation}
class ${className} ${extends} ${implements}
{

    /**
     * table name
     */
    const NAME = '${tableName}';

    ${properties}
}
